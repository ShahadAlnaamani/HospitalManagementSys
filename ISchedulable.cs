using System;
namespace HospitalManagementSys
{
	public interface ISchedulable
    {
        //void ScheduleEvent(Patient patient, Doctor doctor, DateTime appointmentDate, TimeSpan appointmentTime);

        //void ScheduleEvent( DateOnly appointmentDate,TimeSpan appointmentTime, bool book);

      //  void CancelEvent();
      //  void CancelEvent(Appointment appointment);

    }
}
